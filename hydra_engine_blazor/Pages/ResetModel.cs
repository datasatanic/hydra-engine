namespace DefaultNamespace;

public class ResetModel
{
    public string Title { get; set; } = "Reset configuration";
    public string Content { get; set; } = "All your changes will be reset to the state from the Git repository";
}